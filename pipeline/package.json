{
  "name": "domi-data-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 infrastructure skeleton for the Domi Data weekly market-report pipeline (real TypeScript rebuild of the site-data-agent/site-data-monitor-agent Claude routines).",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "@supabase/supabase-js": "^2.45.4",
    "date-fns": "^4.4.0",
    "date-fns-tz": "^3.2.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2",
    "vitest": "^5.0.1"
  }
}
